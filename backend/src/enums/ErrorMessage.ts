enum ErrorMessage {
  NO_FILE = 'Please upload a file',
  SIZE_LIMIT = 'File size cannot be larger than ',
  INVALID_PARAM_GALLERY_IMAGE = 'Invalid param galleryId or imageId',
  IMAGE_NOT_EXISTED = 'Image not existed'
}

export default ErrorMessage;